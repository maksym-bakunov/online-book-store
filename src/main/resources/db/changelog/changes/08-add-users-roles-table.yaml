databaseChangeLog:
    - changeSet:
          id: update-users-roles-table
          author: mpbakunov
          changes:
              - sql:
                    sql: |
                        INSERT INTO users_roles (user_id, role_id)
                        SELECT u.id, r.id
                        FROM users u, roles r
                        WHERE r.name = 'USER';